dist
coverage
node_modules
package-lock.json
THIRD-PARTY-NOTICES.md
public/*.png
public/vite.svg
src/assets

# Synced byte-identically from playbook-engineering and compared by blob SHA.
# Reformatting them here would pass once and be reverted by the next sync, so
# their formatting is owned upstream, not by this repo. See #123.
tools/agent-guard
tools/models

# governed:agent-harness-format:start
# These paths are synchronized by agent-sop. Their lint and syntax
# contract runs at the governance source; consumer formatters must not rewrite
# the managed bytes and start a format-versus-sync loop.
.codex/config.toml
.codex/environments/environment.toml
.codex/rules/environment.rules
.codex/hooks.json
.claude/settings.json
.cursor/hooks.json
.windsurf/hooks.json
# governed:agent-harness-format:end
